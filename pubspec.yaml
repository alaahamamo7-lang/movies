name: movies
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.3.0

  cupertino_icons: ^1.0.8

  carousel_slider: ^5.1.2
  provider: ^6.1.5+1
  flutter_bloc: ^9.1.1
  firebase_auth: ^6.7.0
  firebase_core: ^4.15.0
  google_sign_in: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/icons/
    - assets/images/
    - assets/icons/person/linear/user.svg
